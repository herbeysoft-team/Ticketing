import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

const UnSelectedDate = ({
  text,
  unselectedItemWidth = 38,
  unselectedItemTextStyle = styles.selectedDateText,
  unselectedItemBackgroundColor = '#fff',
  itemRadius,
  itemHeight
}) => {
  return /*#__PURE__*/React.createElement(View, {
    style: [styles.selectedDateContainer, {
      width: unselectedItemWidth,
      backgroundColor: unselectedItemBackgroundColor,
      borderRadius: itemRadius,
      height: itemHeight
    }]
  }, /*#__PURE__*/React.createElement(Text, {
    style: [styles.selectedDateText, unselectedItemTextStyle]
  }, text));
};

const styles = StyleSheet.create({
  selectedDateContainer: {
    width: 38,
    backgroundColor: '#fff',
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
    marginHorizontal: 10
  },
  selectedDateText: {
    fontSize: 16,
    color: '#16213e'
  }
});
export default UnSelectedDate;
//# sourceMappingURL=UnSelected.js.map